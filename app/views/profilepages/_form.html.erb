<%= form_for(@profilepage) do |f| %>
  <% if @profilepage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profilepage.errors.count, "error") %> prohibited this profilepage from being saved:</h2>

      <ul>
      <% @profilepage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stage_name %><br>
    <%= f.text_field :stage_name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
